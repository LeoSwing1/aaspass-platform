# AasPass Admin App

This is the dedicated admin application inside the AasPass monorepo.

Use the shared backend, contracts, auth and UI tokens. Do not create a separate business-rule implementation.
